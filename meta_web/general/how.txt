How the Internet works? (req-res cycle)

Networks, Protocols and servers

Web Browsers & Dev Tools

Front-End, Back-End, Full-Stack

Web Techs and prods

pkg managers, modules, handlers, dep tree, v, frameworks, libs, bundling

web components, rwd UI

deploying

SPA, static & dynamic content, caching, bundling, lazy loading

Accesiblity

Performance

MVC

render, lightweight, reconciliation, DOM, biding, injection, hierarchy

React Fiber Architecture, priority

standard math seq of calc

declaration, datatypes, type casting, coercion, implicit & explicit, dynamic typing, Operator associativity

bug, error(types, handling), warning

classes(base=super, sub), prototype, objects, blueprint, constructors, instantiation, methods

4 fund prins(inheritance, encapsulation, abstraction, polymorphism)

stringfy, parsing, third-party data-converting

testing, TDD

SEO, symantic design, UX, metadata, SM cards

react lib, component-based arch, virtual DOM, DOM manipulation, SPA, UI des, reusability, DRY, indep, 
rendering, speed and performance, spaghetti code, memory, cache

interpreting, transpiler

props, hooks, states

CSS specificity, score calc, selectors, cascading, web-link order (LVHA)

full-stack dev, data stack, caching apps, devOps, UI, workflow, app's core
layers, tiers, N-tier app, client-server arch, hosting, client tier, presentation tier, business logic, thin clients
3-tier arch (most common): presentation tier, app tier, data tier
4-tier arch (when reuired): presentation tier, app tier, data tier, delivery tier, CDN providers
security, scalability, maintenance, improvement, indep, efficiency, configration
malicious content, Validation and sanitization (don't trust anyone)
server capacity, scaling techs, optimization, 

CSS Preprocessors

Errors, Debugging, linters, DevTools, 

SPA, bundling, lazy loading, routing, 